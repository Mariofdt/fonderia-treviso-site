{
  "name": "fonderia-rules-test",
  "description": "Fonderia Treviso — unit test delle Firestore security rules",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --only firestore 'mocha rules.test.js'"
  },
  "dependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0",
    "mocha": "^10.7.3"
  }
}
